apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "excalidraw-refined.fullname" . }}-test-connection"
  labels:
    {{- include "excalidraw-refined.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "excalidraw-refined.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
