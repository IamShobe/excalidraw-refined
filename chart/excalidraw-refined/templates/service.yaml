apiVersion: v1
kind: Service
metadata:
  name: {{ include "excalidraw-refined.fullname" . }}
  labels:
    {{- include "excalidraw-refined.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "excalidraw-refined.selectorLabels" . | nindent 4 }}
